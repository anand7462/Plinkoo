{
    "builds": [
      {
        "src": "server/package.json",
        "use": "@vercel/node",
        "config": {
          "outputDirectory": "server/dist"
        }
      },
      {
        "src": "client/package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "src"
        }
      }
    ],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "/server/dist/$1"
      },
      {
        "src": "/(.*)",
        "dest": "/client/src/$1"
      }
    ]
  }
  